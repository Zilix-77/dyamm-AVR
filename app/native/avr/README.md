# native/avr — AVR emulator (dyamm-AVR) + ATmega32 config (Phase 1).
# FFI via bridge/native_bridge.dart (AvrEngine). Vendored subset lives at
# android/app/src/main/cpp/third_party/simavr (see NATIVE_BUILD.md).
